api_version = 1
name = "BF_GPU_Mesh"
component_kind = "Extension"
type = "Renderer_Extension"
version = { major = 0, minor = 0, patch = 1 }
author = "armscream"
description = "Optional meshlet (task + mesh shader) pipeline extension for BF_GPU."
flags = ["Runtime"]
capabilities = ["Renderer"]
dependency_count = 1

[[dependencies]]
name = "BF_GPU"
min_version = { major = 0, minor = 0, patch = 1 }
max_version = { major = 9, minor = 9, patch = 9 }
has_min_version = true
has_max_version = true
optional = false
